# Capability Layer Structure Check

A valid capability layer may provide:

- `mcp/`
- `skills/`
- `workflows/`
- `subagents/`
- `agents/`
- `registries/`

Secrets must be supplied through environment variables or private local config, not committed into templates.
