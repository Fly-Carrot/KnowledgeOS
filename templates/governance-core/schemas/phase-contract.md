# Phase Contract

KnowledgeOS uses six phase keys:

```text
route -> plan -> review -> dispatch -> execute -> report
```

The sequence is the default lifecycle for substantial work. A small task may compress phases, but it must not silently skip evidence for risky mutation.
