# Cold Archive

This folder is for historical, superseded, or legacy project material that should be stored but not read as default agent context.

Use `knowledgeos archive-legacy-project --write-plan` before moving files here. Agents may inspect `archive/**` only after an explicit human request or a routed archive review task.
